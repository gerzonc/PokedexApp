import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'flex-start',
    marginLeft: 8,
  },
  footer: {
    width: '100%',
    alignItems: 'center',
  },
});
